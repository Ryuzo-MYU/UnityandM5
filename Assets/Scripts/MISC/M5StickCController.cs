using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class M5StickCController : MonoBehaviour
{
    [SerializeField] private M5DataReceiver m5DataReceiver;
    [SerializeField] private bool buttonPushed;
    [SerializeField] private bool buttonWasPushed;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
